language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-react-express' ]; then cd .. && eval "mv $currentfolder generator-react-express" && cd generator-react-express; fi

